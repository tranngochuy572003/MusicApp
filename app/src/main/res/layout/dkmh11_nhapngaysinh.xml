<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@color/white">

    <!-- Nút "Bỏ qua" ở góc trên -->
    <TextView
        android:id="@+id/btnSkip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Bỏ qua"
        android:textColor="@color/gray"
        android:textSize="16sp"
        android:gravity="end"
        android:layout_gravity="end"
        android:layout_marginBottom="24dp" />

    <!-- Tiêu đề "Ngày sinh nhật của bạn?" -->
    <TextView
        android:id="@+id/titleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ngày sinh nhật của bạn?"
        android:textColor="@color/black"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_marginBottom="8dp" />

    <!-- Dòng chú thích nhỏ bên dưới tiêu đề -->
    <TextView
        android:id="@+id/subtitleText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Thông tin của bạn sẽ giúp NCT gợi ý nhạc tốt hơn."
        android:textColor="@color/gray"
        android:textSize="14sp"
        android:layout_marginBottom="16dp" />

    <!-- Khu vực để nhập ngày sinh nhật -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="#e4e4e4"
        android:padding="10dp"
        android:layout_marginBottom="16dp">

        <!-- Ô nhập ngày sinh nhật -->
        <EditText
            android:id="@+id/birthdayEditText"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:hint="Nhập ngày tháng năm sinh"
            android:textSize="16sp"
            android:inputType="none"
            android:focusable="false"
            android:padding="4dp"
            android:background="@android:color/transparent"/>

        <!-- Biểu tượng lịch bên phải ô nhập -->
        <ImageView
            android:id="@+id/calendarIcon"
            android:layout_width="27dp"
            android:layout_height="27dp"
            android:layout_marginStart="8dp"
            android:layout_gravity="center"
            android:contentDescription="Open calendar"
            android:src="@drawable/dkmh6_lich" />
    </LinearLayout>

    <!-- Nút "Tiếp tục" -->
    <Button
        android:id="@+id/btnContinue"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="TIẾP TỤC"
        android:background="@drawable/dk_button_background"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        android:textStyle="bold"
        android:layout_marginTop="24dp"
        app:backgroundTint="@null"/>

</LinearLayout>
